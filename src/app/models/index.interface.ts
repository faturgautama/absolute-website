export interface ContentModel {
    section: string;
    title: string;
    content: string;
    icon?: string;
    image?: string;
}

export interface GalleryModel {
    id: string;
    url: string;
}