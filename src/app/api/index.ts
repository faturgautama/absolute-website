import { environment } from "src/environments/environment";

export const beranda_api = `${environment.firebaseUrl}beranda.json`;
export const beranda_content_api = `${environment.firebaseUrl}beranda/contents.json`;
export const services_content_api = `${environment.firebaseUrl}services.json`;
export const news_content_api = `${environment.firebaseUrl}news.json`;
export const news_by_id_content_api = `${environment.firebaseUrl}news/content/`;
export const gallery_content_api = `${environment.firebaseUrl}gallery.json`;